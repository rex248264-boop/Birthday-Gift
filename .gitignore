node_modules
package-lock.json
dist
dist-ssr
*.local
.vite
.DS_Store
.env
.env.local
*.log

# tsc -b artefacts from `npm run build` (vite.config.ts gets emitted as JS;
# if left around, vite picks the .js over the .ts and ignores plugin edits).
vite.config.js
vite.config.d.ts
*.tsbuildinfo
